# Hyprland Configuration - KISS (Keep It Simple, Stupid)

# WayVNC server for remote desktop access
# Use 0.0.0.0 to expose to network, or localhost for SSH tunnel only
exec-once = wayvnc 0.0.0.0 5900
